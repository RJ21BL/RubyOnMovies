<center>
<h1>Movies</h1>

<%= form_tag(movies_path, method: 'get') do %>
      <%= text_field_tag(:search_bar, params[:search_bar]) %>
      <%= submit_tag("Search movie", name: nil) %>
<% end %>

<% @genres.each do |genre| %>
  <%= link_to genre.name, movies_path %>
  
<% end %>

</br>
</br>
</br>
</br>
<% @movies.each do |movie| %>
  <%= movie.id %>
  <%= link_to movie.title, movie %>
  <%= movie.year %> </br>
    <strong><%= link_to 'DELETE', movie, method: :delete, data: { confirm: 'Are you sure you want to delete this movie?' }  %> </strong>
    <strong><%= link_to 'EDIT', edit_movie_path(movie) %></strong>
      <p>
<% end %>

<h2><%= link_to 'Add A Movie', new_movie_path %></h2>
</center>
